resources:
  jobs:
    hello_world_job:
      name: ${bundle.name}_hello_world

      tasks:
        - task_key: running_hello_world_python

          spark_python_task:
            python_file: ../src/hello.py

          environment_key: default

      environments:
        - environment_key: default
          spec:
            environment_version: "2"
            